pyodbc
python-dotenv>=1.0
pillow
numpy
pandas
SQLAlchemy>=2,<3
requests>=2.32
groq>=0.9.0
passlib
openai
anthropic>=0.40
werkzeug>=3.0
pymysql>=1.1
psycopg2-binary>=2.9

# Leitura das contas fixas em PDF (import_email_custos_ti.py / contas_pdf.py).
# Só inspeciona o PDF; quem extrai texto é o poppler e quem lê imagem é o
# tesseract — dois pacotes DE SISTEMA, não entram aqui:
#   apt install poppler-utils tesseract-ocr tesseract-ocr-por
pypdf>=6.0
itsdangerous
